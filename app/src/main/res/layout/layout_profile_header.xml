<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/header_bottom_rounded"
    android:padding="16dp">

    <!-- FOTO DO USUÁRIO -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardProfile"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_marginStart="8dp"
        app:cardCornerRadius="35dp"
        app:cardElevation="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/profileImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/user_default_foreground" />
    </androidx.cardview.widget.CardView>

    <!-- TÍTULO -->
    <TextView
        android:id="@+id/greetingTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:fontFamily="@font/poppins_semibold"
        android:text="Olá, Estudante!"
        android:textColor="@color/white"
        android:textSize="18sp"
        app:layout_constraintStart_toEndOf="@id/cardProfile"
        app:layout_constraintTop_toTopOf="@id/cardProfile" />

    <!-- SUBTÍTULO -->
    <TextView
        android:id="@+id/greetingSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:fontFamily="@font/poppins"
        android:text="O que vai ser hoje?"
        android:textColor="#DDF0F0F0"
        android:textSize="13sp"
        app:layout_constraintStart_toStartOf="@id/greetingTitle"
        app:layout_constraintTop_toBottomOf="@id/greetingTitle" />

</androidx.constraintlayout.widget.ConstraintLayout>
